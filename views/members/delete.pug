extends ../layout

block content
  h2= title
  p 
    | Are you sure you want to delete 
    em= member.title
    | ?
  form(action="/member/" + member.id + "/delete", method="post")
    a(href="/member/" + member.id) No! Take me back! 
    | 
    input(type="submit", value = "Yes, I'm sure")